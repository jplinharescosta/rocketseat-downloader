# Credenciais (opcional – pode deixar vazio para ser solicitado no prompt)
ROCKETSEAT_EMAIL=
ROCKETSEAT_PASSWORD=

# HTTP
HTTP_TIMEOUT=30

# CDN
CDN_DOMAIN=vz-dc851587-83d.b-cdn.net

# Sessão (onde salvar o pickle da sessão autenticada)
SESSION_DIR=.

# Sincronização pós-formação (rclone)
# Habilitar: 1|true|yes desabilitar: 0|false|no
RCLONE_SYNC=0
RCLONE_BIN=rclone
RCLONE_REMOTE=gdrive
RCLONE_DEST=Cursos
# copy (copia), sync (sincroniza), move (move/apaga origem após copiar)
RCLONE_MODE=copy
# specialization (pasta da formação) | all (pasta Cursos inteira)
RCLONE_SCOPE=specialization
RCLONE_TRANSFERS=4
RCLONE_CHECKERS=8
RCLONE_CHUNK_SIZE=64M
RCLONE_RETRIES=5
RCLONE_LL_RETRIES=10
RCLONE_STATS=30s
# Ex.: --progress --color never (NÃO use --dry-run se quiser executar de verdade)
RCLONE_BWLIMIT=
RCLONE_EXTRA_ARGS=
